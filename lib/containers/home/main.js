import { StyleSheet, } from 'react-native';

const styles = StyleSheet.create({
  CurrentWeatherContainer: {
    flex: -1,
    alignItems: 'center',
    marginLeft: 50,
  },
  CurrentWeatherText: {
    fontSize: 24,
  },
});
