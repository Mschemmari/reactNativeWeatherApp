import { StyleSheet, } from 'react-native';

export const main = StyleSheet.create({
  CurrentWeatherContainer: {
    flex: -1,
    alignItems: 'center',
    backgroundColor: '#b578b3'
  },
  CurrentWeatherText: {
    fontSize: 80,
    color: '#fff'
  },
});
